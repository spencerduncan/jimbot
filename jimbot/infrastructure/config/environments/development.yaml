# Development environment overrides
infrastructure:
  event_bus:
    batch_window_ms: 50  # Faster batching for development
    
  monitoring:
    profiling_enabled: true  # Enable profiling in dev
    
  logging:
    level: DEBUG  # More verbose logging